# Reels_Download_TelegramBot

Телеграм бот для скачивания видео из **TikTok**, **Instagram Reels/Posts**, и **YouTube Shorts**. Вставляете ссылку в бота, он возвращает готовое видео. Скачивайте и делитесь BrainRot видео со своими друзьями (надеюсь они будут рады тому что вы скидываете им тикитоки).

## 🚀 Функционал

- **Поддержка платформ**: YouTube (Shorts), Instagram (Reels, Posts), TikTok
- **Кэширование**: Видео кэшируются на серверах Telegram (через приватный канал), что обеспечивает мгновенную доставку для повторных запросов
- **Inline Query**: Используйте `@botname` в любом чате для быстрого доступа к видео
- **Lock механизм**: Предотвращает дублирование скачиваний - если 100 пользователей запрашивают одно видео, скачивается только 1 раз
- **Background Workers**: Отдельные процессы для обработки задач на скачивание, что позволяет боту быстро отвечать на запросы
- **TTL кэша**: Автоматическая очистка кэша через 7 дней (Redis)
- **Канонический ID**: Использует `yt-dlp` для извлечения канонического ID видео, предотвращая дубликаты в кэше для разных URL одного видео

## 📋 Требования

- **Python 3.8+**
- **Redis** (для кэширования и очереди задач)
- **Telegram Bot Token** (получить у [@BotFather](https://t.me/BotFather))
- **Telegram Channel ID** (приватный канал для хранения видео, бот должен быть админом)

## 🛠 Установка

### 1. Клонирование репозитория

```bash
git clone https://github.com/lcaitlyn/Reels_Download_TelegramBot.git
cd Reels_Download_TelegramBot
```

### 2. Создание виртуального окружения

```bash
python3 -m venv venv
source venv/bin/activate  # Для macOS/Linux
# или
venv\Scripts\activate  # Для Windows
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Установка Redis

**macOS**:
```bash
brew install redis
brew services start redis
```

**Linux (Ubuntu/Debian)**:
```bash
sudo apt-get update
sudo apt-get install redis-server
sudo systemctl start redis
```

**Windows**:
Скачайте Redis с [официального сайта](https://redis.io/download) или используйте WSL.

**Проверка работы Redis**:
```bash
redis-cli ping
# Должно вернуть: PONG
```

### 5. Настройка переменных окружения

Создайте файл `.env` в корневой директории проекта:

```bash
cp .env.example .env  # Если есть пример
# или создайте вручную
```

Отредактируйте `.env`:

```env
BOT_TOKEN=your_bot_token_here
TELEGRAM_CHANNEL_ID=your_channel_id_here
REDIS_URL=redis://localhost:6379/0
```

**Как получить BOT_TOKEN**:
1. Найдите [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot` и следуйте инструкциям
3. Скопируйте токен в `.env`

**Как получить TELEGRAM_CHANNEL_ID**:
1. Создайте приватный канал в Telegram
2. Добавьте бота в канал как администратора
3. Найдите ID канала через [@userinfobot](https://t.me/userinfobot) или [@RawDataBot](https://t.me/RawDataBot)

## ▶️ Запуск

### Вариант 1: Бот + Worker (рекомендуется для продакшена)

Проект состоит из двух компонентов:

1. **Bot** (`main.py`) - принимает запросы от пользователей, проверяет кэш, добавляет задачи в очередь
2. **Worker** (`worker.py`) - обрабатывает задачи на скачивание видео из очереди Redis

Запустите в двух терминалах:

**Терминал 1 - Bot**:
```bash
python main.py
```

**Терминал 2 - Worker**:
```bash
python worker.py
```

Можно запустить несколько workers для параллельной обработки задач:
```bash
# Терминал 2
python worker.py

# Терминал 3 (опционально, для масштабирования)
python worker.py

# Терминал 4 (опционально)
python worker.py
```

### Вариант 2: Только Bot (для тестирования)

Если не запущен worker, бот все равно будет работать, но скачивание видео будет происходить синхронно (медленнее).

```bash
python main.py
```

## 📁 Структура проекта

```
.
├── bot.py              # Основной модуль бота (handlers, inline query)
├── worker.py           # Background worker для обработки задач из очереди
├── database.py         # Работа с Redis (кэш, очередь, locks)
├── downloader.py       # Модуль скачивания видео через yt-dlp
├── utils.py            # Утилиты (нормализация URL, определение платформы)
├── main.py             # Точка входа для бота
├── requirements.txt    # Python зависимости
├── .env                # Переменные окружения (создается вручную)
├── .gitignore          # Git ignore файлы
└── downloads/          # Временная директория для скачанных видео
```

## 🔧 Архитектура

```
┌─────────┐      ┌──────────┐      ┌─────────┐
│  Бот    │───►  │  Redis   │◄───  │ Worker  │
│ (бот)   │      │ очередь  │      │(процесс)│
└─────────┘      └──────────┘      └─────────┘
     │                  │                  │
     └──────────────────┴──────────────────┘
                        │
                   ┌─────────┐
                   │ Telegram│
                   │  Channel│
                   │  (кэш)  │
                   └─────────┘
```

1. **Пользователь** отправляет ссылку → **Bot** проверяет кэш в Redis
2. Если видео **в кэше** → отправляется сразу (мгновенно)
3. Если видео **не в кэше** → задача добавляется в очередь Redis → **Worker** скачивает → сохраняет в кэш → отправляет пользователю
4. Видео хранится в **Telegram Channel** (кэш на серверах Telegram)

## ⚙️ Настройки

### Redis TTL (Time To Live)

Кэш автоматически удаляется через 7 дней без использования. Можно изменить в `database.py`:

```python
TTL_SECONDS = 7 * 24 * 60 * 60  # 7 дней
```

### Lock Timeout

Максимальное время скачивания видео (30 минут). Можно изменить в `database.py`:

```python
LOCK_TTL_SECONDS = 30 * 60  # 30 минут
```

### Качество видео

Настройки качества видео для разных платформ можно изменить в `downloader.py` (метод `_get_format_for_platform`).

## 📝 Использование

### Обычный запрос
Отправьте ссылку на видео боту в личные сообщения:
```
https://www.instagram.com/reel/ABC123/
```

### Inline Query
В любом чате наберите:
```
@botname https://www.tiktok.com/@user/video/1234567890
```

Выберите результат из списка - видео будет отправлено в чат.

## 🐛 Troubleshooting

**Проблема**: `BOT_TOKEN not found`
- Решение: Убедитесь, что файл `.env` создан и содержит `BOT_TOKEN=...`

**Проблема**: `TELEGRAM_CHANNEL_ID not found`
- Решение: Добавьте `TELEGRAM_CHANNEL_ID=...` в `.env` (ID канала или username)

**Проблема**: `Connection refused` к Redis
- Решение: Убедитесь, что Redis запущен: `redis-cli ping` (должно вернуть `PONG`)

**Проблема**: Видео скачивается медленно
- Решение: Запустите worker отдельно (`python worker.py`) для фоновой обработки

**Проблема**: `externally-managed-environment` ошибка
- Решение: Используйте виртуальное окружение (`python3 -m venv venv` и `source venv/bin/activate`)

## 📄 Лицензия

MIT (хз ваще зачем нужна, кто этот говно-код будет тырить 🤡)

## 👤 Автор

lcaitlyn (навайбкодил вместе с Cursor за 20$/мес)
